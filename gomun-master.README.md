# 古文 학습 마스터 (고문 마스터)

고문·한문 원문을 **읽기 → 현대어 풀이 → 어휘 → 문법 → 문장 구조 → 퀴즈 → 오답 복습**의
흐름으로 학습하는 단일 HTML 교육용 웹앱입니다. 빌드 과정 없이 `gomun-master.html` 하나로
바로 실행됩니다.

## 실행 방법

1. `gomun-master.html`을 브라우저로 엽니다(더블클릭). 인터넷 연결이 있으면
   Tailwind / Chart.js / Font Awesome / Google Fonts(CDN)가 자동 로드됩니다.
2. 첫 작품이 자동 로드되어 빈 화면 없이 바로 체험할 수 있습니다.
3. 정적 호스팅(GitHub Pages 등)에 올리려면 이 파일을 `index.html`로 복사해도 됩니다.

### 오프라인판 (`gomun-master.offline.html`)

인터넷·CDN 없이 어디서나 동일하게 동작하는 **완전 자립형(self-contained)** 빌드입니다.
교내망·시험장·오프라인 시연용으로 이 파일을 그대로 사용하세요.

- Tailwind CSS(빌드본), Chart.js, Font Awesome(웹폰트 base64)을 파일에 모두 내장했습니다.
  네트워크가 전혀 없어도 레이아웃·아이콘·차트가 정상 렌더됩니다.
- 한글/한문 본문 폰트(Google Fonts)만 외부 링크로 두었으며, 오프라인 시에는
  시스템 명조/고딕으로 자연스럽게 대체됩니다(CJK 폰트 내장 시 파일이 10MB+로 커지기 때문).
- `gomun-master.html`(CDN판)이 원본이며, 오프라인판은 그로부터 생성된 배포 사본입니다.
  기능·데이터는 두 파일이 동일합니다.

## 조작법

- **카드 클릭 / Space**: 원문 ↔ 해석 뒤집기
- **← / →**: 이전 / 다음 문장
- **하단 도구**: 북마크 · 단어장 추가 · 해석/원문 가리기 · 퀴즈 · 복습 · 학습 완료
- **사이드바**: 카테고리·작품 이동, 대시보드 / 단어장 / 오답 노트 / 배지
- **관리자**: 헤더 ⚙️ → 기본 비밀번호 `admin123` (관리자 화면에서 변경 가능)

## 주요 기능

| 영역 | 내용 |
|------|------|
| 데이터 | **총 약 396문장 / 62작품**. 짧은 대구는 **완전한 문장으로 병합**(예: `父生我身, 母鞠吾身`)하여 자연스러운 한 문장 단위로 구성. **유명 명문은 전문(全文) 수록** — 출사표(45)·귀거래사(16)·난정집서(15)·애련설(11)·사설(10), 당시 전편(춘망·정야사·강설·등관작루 등). 그 밖에 사자소학·명심보감·천자문·논어·맹자·노자·장자·대학·중용·채근담·순자 등. 각 문장에 원문·독음·직역·현대어풀이·핵심어휘·문법·문장구조·배경지식·시험포인트·퀴즈·**문장 길이** 포함 |
| 학습 | 플래시카드(3D 뒤집기), 핵심 어휘 자동 강조, 통합 검색, 갈래·난이도·**문장 길이** 필터 |
| 문장 길이 | 헤더의 **길이 필터**로 짧게(4자 이하)·보통(5~6자)·길게(7자 이상) 선택. 카드마다 글자 수 배지 표시 |
| 퀴즈 | 해석선택·어휘·문법·빈칸·OX·작품명 등, 즉시 정오답 피드백, 오답 자동 저장 |
| 복습 | 간격 반복(1·3·7·16일) + 오답 집중 복습, 만기 문장 자동 수집 |
| 개인화 | 단어장(목록 생성/메모), 오답 노트, 북마크, 오늘의 목표, 연속 학습 streak |
| 통계 | Chart.js 대시보드 — 최근 14일·작품별 진도·문법별 정답률·오답률 |
| 동기부여 | 성취 배지 12종, 획득 토스트 알림 |
| 관리자 | JSON으로 작품 추가, API 키 설정, JSON 내보내기/가져오기, LocalStorage 초기화 |
| AI 해설 | Claude / OpenAI 연동(키 미설정 시 내장 11단계 데모 해설로 폴백) |

## 데이터 저장

모든 학습 상태는 브라우저 **LocalStorage**(`gomun_master_state_v1`)에 저장됩니다.
관리자 화면의 **JSON 내보내기/가져오기**로 백업·이전이 가능하며, **초기화** 버튼으로
모든 데이터를 리셋할 수 있습니다.

## 데이터 정확도 안내

500문장 중 핵심 필드(**원문·현대어 풀이·출전**)는 검증된 고전(사자소학·명심보감·천자문·
사서·노장·당시 등)에서 큐레이션했습니다. 부가 필드(**독음·핵심 어휘·문법 포인트·문장 구조·
퀴즈**)는 약 640자 규모의 한자 사전과 규칙으로 **자동 생성**되었습니다. 자동 생성 필드는
교육 자료로 활용하기 전 교사·전문가의 검토를 권장합니다. (상세 큐레이션된 처음 25문장은
어휘·문법 해설까지 직접 작성된 것입니다.)

## 보안 안내 (AI 해설)

프로토타입은 편의를 위해 API 키를 LocalStorage에 저장하고 브라우저에서 직접 호출합니다.
**실제 서비스 배포 시에는 키가 클라이언트에 노출되지 않도록 서버 환경변수 + 프록시 방식**을
사용해야 합니다. (앱 내에도 동일한 안내가 표시됩니다.)

## 기술 스택

순수 HTML / CSS / JavaScript + Tailwind CSS(CDN) · Chart.js · Font Awesome · Google Fonts.
프레임워크·빌드 도구·서버가 필요 없습니다.

## 검증

Playwright 스모크 테스트로 초기 렌더, 카드 뒤집기, 문장 이동, 북마크, 학습 완료,
단어장 추가, 퀴즈 풀이·피드백, 검색, 대시보드 차트, 배지, 뷰 전환 등 17개 핵심 흐름이
정상 동작함을 확인했습니다. 또한 총 문장 수 500, 문장 길이 필드·길이 필터(짧게/보통/길게),
생성 데이터의 독음·퀴즈 정확성 등 10개 항목을 추가로 검증했습니다(페이지 에러 0건).
